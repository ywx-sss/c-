#include <bits/stdc++.h>
using namespace std;
string jiafa(string a,string b){
	for(int i=1;i<=b.size()-1;i++){
		a[a.size()-1];
	}
}
int main()
  string a,b;
  cin>>a>>b;
  cout<<a[a.size()-1]+b[b.size()-1];
  return 0;
}